    // Generar un número aleatorio entre 1 y 100
/*     
    const random = Math.floor(Math.random() * 100) + 1;
    let tries = 0;

    function checkGuess() {
      const respuestaUsuario = parseInt(document.getElementById("guessInput").value);
      tries++;

      if (guess === randomNumber) {
        document.getElementById("message").textContent = `¡Correcto! ¡Adivinaste en ${tries} intentos!`;
      } else if (guess < randomNumber) {
        document.getElementById("message").textContent = "Más alto. Intenta de nuevo.";
      } else {
        document.getElementById("message").textContent = "Más bajo. Intenta de nuevo.";
      }
    }
  */

/* function saludar() {
  let nombre = console.log ("Bienvenido cual es su nombre");
  if (nombre == 5) {
      alert(" Hola en que podemos ayudarte " + nombre);   
  }   else {
      alert(`${nombre} no es valido`)
  }    
}
   */

let nombre = console.log("hola raul")